<%= form_for(@coin_set) do |f| %>
  <% if @coin_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coin_set.errors.count, "error") %> prohibited this coin_set from being saved:</h2>

      <ul>
      <% @coin_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
